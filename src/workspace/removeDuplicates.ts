// @ts-nocheck
// ═══════════════════════════════════════════════════════════════
// 🎯 REMOVE DUPLICATES
// ═══════════════════════════════════════════════════════════════
// Write a function that removes duplicate values from an array.
// Try multiple approaches: filter+indexOf, Set, reduce
// ═══════════════════════════════════════════════════════════════

export const run = () => {
  const removeDuplicates = (arr) => {
    // Your implementation here
  };

  // Tests:
  const myArray = [1, 2, 3, 4, 1, 2, 5, 3];
  console.log(removeDuplicates(myArray));
  // Expected: [1, 2, 3, 4, 5]

  // Bonus: try with filter + indexOf
  // Bonus: try with Set
  // Bonus: try with reduce
};
